const mutations={
    increament(state,num){
        state.num = state.num + num;

    },
    // response_login(state,response){
    //     state.loginres = response;
    //     console.log(state.loginres);
    // }
}
export default mutations